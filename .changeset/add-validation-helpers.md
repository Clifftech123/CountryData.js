---
"countrydata.js": minor
---

Add validation helpers: `Country.isValidCountryCode(code)`, `Country.isValidPhoneCode(code)`, and `State.isValidStateCode(stateCode, countryCode)`. Also available as `CountryHelper` instance methods.
