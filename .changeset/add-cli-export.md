---
"countrydata.js": minor
---

Add a `countrydata-export` CLI (via `npx countrydata-export`) that dumps countries/states/cities as CSV or SQL for non-JS consumers: `--entity <countries|states|cities>`, `--format csv|sql`, `--out <path>`. No new dependency — pure Node built-ins.
